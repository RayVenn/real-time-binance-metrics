apiVersion: v1
kind: Secret
metadata:
  name: portfolio-optimizer-secret
  labels:
    {{- include "portfolio-optimizer.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
type: Opaque
# The actual value must be set manually:
#   kubectl create secret generic portfolio-optimizer-secret \
#     --from-literal=api-key=<YOUR_API_KEY> -n portfolio-optimizer
# For production, use External Secrets Operator or Sealed Secrets.
